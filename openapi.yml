openapi: 3.0.3
info:
  title: API Quest API
  version: 0.1.0
  description: API Quest assessment service.

tags:
  - name: Auth
    description: Authentication and token issuance.
  - name: Books
    description: Create, read, update, and delete books.
  - name: Echo
    description: Echoes request payloads back to the client.
  - name: Health
    description: Service health checks.

paths:
  /:
    get:
      tags:
        - Health
      summary: Check service health at the root path.
      responses:
        "200":
          description: Service is healthy.
  /auth/token:
    post:
      tags:
        - Auth
      summary: Issue an access token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: The issued access token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "400":
          description: The request body failed validation.
        "401":
          description: The credentials are invalid.
  /books:
    get:
      tags:
        - Books
      summary: List all books.
      security:
        - BearerAuth: []
      parameters:
        - name: author
          in: query
          required: false
          description: Case-insensitive substring used to filter books by author.
          schema:
            type: string
        - name: page
          in: query
          required: false
          description: One-based page number to return.
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          required: false
          description: Maximum number of books to return per page.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
      responses:
        "200":
          description: A page of books matching the filter.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Book"
        "400":
          description: A query parameter failed validation.
        "401":
          description: The access token is missing or invalid.
    post:
      tags:
        - Books
      summary: Create a book.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBookRequest"
      responses:
        "201":
          description: The created book.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Book"
        "400":
          description: The request body failed validation.
  /books/{id}:
    delete:
      tags:
        - Books
      summary: Delete a book.
      parameters:
        - name: id
          in: path
          required: true
          description: Identifier of the book.
          schema:
            type: string
      responses:
        "204":
          description: The book was deleted.
        "404":
          description: No book has the given id.
    get:
      tags:
        - Books
      summary: Get a single book by id.
      parameters:
        - name: id
          in: path
          required: true
          description: Identifier of the book.
          schema:
            type: string
      responses:
        "200":
          description: The requested book.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Book"
        "404":
          description: No book has the given id.
    put:
      tags:
        - Books
      summary: Update a book.
      parameters:
        - name: id
          in: path
          required: true
          description: Identifier of the book.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBookRequest"
      responses:
        "200":
          description: The updated book.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Book"
        "400":
          description: The request body failed validation.
        "404":
          description: No book has the given id.
  /echo:
    post:
      tags:
        - Echo
      summary: Echo the JSON object from the request body.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: The same JSON object that was received.
          content:
            application/json:
              schema:
                type: object
        "400":
          description: The request body is missing or not valid JSON.
  /health:
    get:
      tags:
        - Health
      summary: Check service health.
      responses:
        "200":
          description: Service is healthy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
  /ping:
    get:
      tags:
        - Health
      summary: Check whether the service is alive.
      responses:
        "200":
          description: Service is alive.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean

components:
  schemas:
    Book:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the book.
        title:
          type: string
          description: Title of the book.
        author:
          type: string
          description: Author of the book.
        year:
          type: integer
          description: Publication year of the book.
      required:
        - id
        - title
        - author
        - year
    CreateBookRequest:
      type: object
      properties:
        title:
          type: string
          description: Title of the book.
        author:
          type: string
          description: Author of the book.
        year:
          type: integer
          description: Publication year of the book.
      required:
        - title
        - author
        - year
    LoginRequest:
      type: object
      properties:
        username:
          type: string
          description: Username submitted by the client.
        password:
          type: string
          description: Password submitted by the client.
      required:
        - username
        - password
    TokenResponse:
      type: object
      properties:
        token:
          type: string
          description: Signed JWT bearer token.
      required:
        - token
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
